{
    "$schema": "./config.schema.json",
    "homeScreenSections": [
        {
            "type": "mediaBar",
            "enabled": true,
            "size": "medium",
            "items": {
                "sortBy": ["DateCreated"],
                "sortOrder": "Descending",
                "limit": 10
            }
        },
        {
            "type": "recentlyAdded",
            "enabled": true
        }
    ]
}
